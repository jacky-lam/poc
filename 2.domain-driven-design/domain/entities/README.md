domain/entities:

Each domain model (entity) is defined here.

For example, OnRoadExperiment might have a method like mark_complete() that changes its internal status to "completed" but only if certain domain rules are met.
